// Shared model identifiers. Single source of truth so the chat route, the
// summarizer, the response cache (version hash) and the Gemini context-cache
// helper all agree on which models are in play.
export const GEMINI_MODEL_ID = "gemini-2.5-flash";
export const SONNET_MODEL_ID = "claude-sonnet-4-6";
